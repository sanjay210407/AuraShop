services:
  - type: web
    name: aurashop-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FRONTEND_URL
        value: https://your-frontend-domain.vercel.app
      - key: SESSION_SECRET
        generateValue: true
